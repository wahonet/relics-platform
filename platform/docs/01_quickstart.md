# 快速入门

（B/E 阶段完善）

## 5 分钟跑起来

1. 双击 `setup.bat`
2. 用记事本打开 `config.yaml`，修改 `project.name` 和 `geo.center`
3. 将档案 DOCX 放入 `data/input/01_archives/`
4. 双击 `run_pipeline.bat`
5. 双击 `start_platform.bat`，浏览器自动打开
