# relics-platform

最小说明：

1. `setup.bat`
2. `run_pipeline.bat`
3. `start_platform.bat`

默认地址：

- `http://127.0.0.1:8000`

目录只看这几个：

- `config.yaml`
- `data/input`
- `data/output`
- `platform/webgis`

其余按需处理。
